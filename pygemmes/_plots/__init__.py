'''
Contains all plots one can use with a hub that has run (some require additional calculation)
use tab to see which are available
use ? on each of its contents
or use `pgm.get_available_plots()`
'''
from ._plots import *
